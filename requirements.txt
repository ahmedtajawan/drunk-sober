streamlit==1.11.1          # Compatible with streamlit-webrtc
streamlit-webrtc==0.42.0    # Required for your application
protobuf==3.20.3            # Required for compatibility with older streamlit
librosa==0.10.0             # Latest version; Compatible with numpy>=1.20.0
numpy==1.22.4               # Compatible with librosa and scikit-learn
pydub==0.25.1               # Works well, requires ffmpeg installed
wavio==0.0.9                # Works for audio saving
scikit-learn==1.0.2         # Recommended for compatibility with your model
av==14.2.0                  # Required for audio processing with webrtc
altair==4.1.0               # Compatible with older Streamlit versions
